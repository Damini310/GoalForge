spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

server:
  port: 8080

app:
  jwt:
    secret: vaultapi_super_secret_key_uthman_2024
    expiration: 86400000